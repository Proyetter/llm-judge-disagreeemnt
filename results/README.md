# Results

Captured stdout of each analysis script, regenerated by `../run_analysis.sh`.

| File | Paper table |
| --- | --- |
| `audit.txt` | Methods: eligibility counts and parse-failure rates |
| `table2_decomposition.txt` | Table II |
| `table3_instability_categories.txt` | Table III, category shares |
| `table3_conditional_noise.txt` | Table III, conditional noise rates |
| `table4_confidence_auroc.txt` | Table IV, single-shot columns |

The files committed here are the outputs the paper reports. Point estimates
should reproduce exactly. Bootstrap interval endpoints will move slightly
between runs, since the resampling is not seeded.

TODO: run ../run_analysis.sh once after assembling the repository, then commit
whatever lands here. Before committing, spot-check two or three numbers against
the paper tables. If anything disagrees, the paper and the repository have
diverged and that needs resolving before submission.
